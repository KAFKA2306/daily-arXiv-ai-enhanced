# .vscode ディレクトリ

VS Code 用のワークスペース設定を格納します。Live Preview で `index.html` を自動的に開くよう指定しています。

## コマンド例
- `code .` で現在のプロジェクトをVS Codeに読み込みます。

## メモ
- `livePreview.defaultPreviewPath` を変更するときはチーム内で共有してください。
- エディタごとの設定差異がある場合は、このフォルダで制御します。
